name: card_matching_game
description: "A feature-rich Flutter card-matching game using online image links."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  confetti: ^0.8.0
  audioplayers: ^6.3.0
  vibration: ^3.1.3
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
